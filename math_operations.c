#include "monty.h"

/**
 * _add - adds the top two elements of the stack.
 * If the stack contains less than two elements, handle error
 * with _error(8).
 * @line_number: line number of the opcode in the file.
 * Return: void.
 */
void _add(stack_t **stack, unsigned int line_number)
{

}

/**
 * _sub - subtracts the top element of the stack from
 * the second top element of the stack.
 * If the stack contains less than two elements, handle error
 * with _error(8).
 * @line_number: line number of the opcode in the file.
 * Return: void.
 */
void _sub(stack_t **stack, unsigned int line_number)
{

}

/**
 * _div - divides the second top element of the stack
 * by the top element of the stack.
 * If the stack contains less than two elements, handle error
 * with _error(8).
 * If the top element of the stack is 0, div or mod by 0 is not allowed.
 * handle error with _error(10).
 * @line_number: line number of the opcode in the file.
 * Return: void.
 */
void _div(stack_t **stack, unsigned int line_number)
{

}

/**
 * _mul - multiplies the second top element of the stack
 * with the top element of the stack.
 * If the stack contains less than two elements, handle error
 * with _error(8).
 * @line_number: line number of the opcode in the file.
 * Return: void.
 */
void _mul(stack_t **stack, unsigned int line_number)
{

}

/**
 * _mod - computes the rest of the division of the second
 * top element of the stack by the top element of the stack.
 * If the stack contains less than two elements, handle error
 * with _error(8).
 * If the top element of the stack is 0, div or mod by 0 is not allowed.
 * handle error with _error(10).
 * @line_number: line number of the opcode in the file.
 * Return: void.
 */
void _mod(stack_t **stack, unsigned int line_number)
{

}
